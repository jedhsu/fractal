\\\!

    *At*

  Abstract type for a parameter schedule, which represents a function from
  nonnegative i32egers to numbers of type `R`. Subtypes must implement the
  
  `getindex(s::AbstractSchedule, i::Int)` operator.

\\\!


pub struct At:
    pass